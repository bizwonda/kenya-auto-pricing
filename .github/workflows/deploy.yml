name: Deploy API

# Disabled until you connect Railway/Render
# Uncomment and add secrets when ready to deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v4

      - name: Deploy placeholder
        run: echo "Add RAILWAY_TOKEN or RENDER_DEPLOY_HOOK secret, uncomment below"

      # - name: Deploy to Railway
      #   uses: railwayapp/cli@latest
      #   with:
      #     args: up --service kenya-auto-pricing --detach
      #   env:
      #     RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

      # - name: Deploy to Render
      #   run: |
      #     curl -X POST "https://api.render.com/deploy/${{ secrets.RENDER_DEPLOY_HOOK }}"
